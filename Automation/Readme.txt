This repository contains a script which opens a text file with all the ip addresses allowed to access the network and removes the ones that are to be blocked.
This script has also been turned into a function so it can be reused easily.
